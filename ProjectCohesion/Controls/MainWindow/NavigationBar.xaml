<UserControl x:Class="ProjectCohesion.Controls.MainWindow.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectCohesion.Controls.MainWindow"
             xmlns:winui="clr-namespace:ProjectCohesion.Win32.Controls;assembly=ProjectCohesion.Win32" 
             xmlns:viewmodels="clr-namespace:ProjectCohesion.Core.ViewModels;assembly=ProjectCohesion.Core" d:DataContext="{d:DesignInstance Type=viewmodels:AppViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="0,0,0,1" BorderBrush="#e6e6e6">
        <StackPanel>
            <winui:NavigationView 
                Margin="12,0,12,0" 
                x:Name="navigationView" 
                MenuItems="{Binding UIViewModel.TopMenu.Items}" 
                SelectedItem="{Binding UIViewModel.TopMenu.Selected, Mode=TwoWay}"/>
            <Border Height="80" Background="White" BorderBrush="#e6e6e6" BorderThickness="1" CornerRadius="6" Margin="12,0,12,8">

            </Border>
        </StackPanel>
    </Border>

</UserControl>
